# Configuration

The executable Python files contain the authoritative defaults used for the
published experiments. They are intentionally kept next to the implementations
to prevent an unused configuration file from drifting away from the code.

The complete settings, checkpoint mapping, and seed list are summarized in
[`docs/reproducibility.md`](../docs/reproducibility.md).
